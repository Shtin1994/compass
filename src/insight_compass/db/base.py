# src/insight_compass/db/base.py

# ==============================================================================
# ЦЕНТРАЛЬНАЯ ТОЧКА ДЛЯ ОБНАРУЖЕНИЯ МОДЕЛЕЙ
# ==============================================================================
# Этот файл играет ключевую роль в работе системы миграций Alembic.
# Его единственная задача — импортировать базовый класс `Base` и АБСОЛЮТНО ВСЕ
# модели ORM, которые существуют в проекте.
#
# Когда мы запускаем `alembic revision --autogenerate`, Alembic смотрит
# на метаданные, привязанные к `Base`, и сравнивает их с текущей структурой
# базы данных. Благодаря этому файлу, ему не нужно "искать" модели по всему
# проекту — все они собраны здесь. Это делает процесс создания миграций
# надежным и предсказуемым.
# ==============================================================================

# Импортируем базовый декларативный класс.
from insight_compass.db.base_class import Base

# Импортируем все модели данных Telegram.
# Alembic увидит их все отсюда.
from insight_compass.models.telegram_data import (
    Channel,
    Post,
    Comment,
    TelegramUser,
    TelegramAccount
)

# Импортируем модели из других модулей приложения.
# Примечание: Если этих файлов/моделей у вас еще нет, их можно закомментировать,
# но лучше оставить для полноты архитектуры.
from insight_compass.models.ai_analysis import PostAnalysis
# from insight_compass.models.outbox import OutboxTask